# yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json

kind: AIchorManifest
apiVersion: 0.2.2

builder:
  image: image
  context: smoke-test # smoke-test folder
  dockerfile: ./build/Dockerfile

spec:
  operator: jobset
  image: image
  command: "python3 -u main.py --operator=jobset --sleep=300 --tb-write=True"
  storage:
   attachExistingPVCs:
    - name: mchennoufi-d-bb31067bee564485-kyber-pvc-306efbf01f44427d
      mountPoint: /mnt/mchennoufi-d-bb31067bee564485-kyber-pvc-306efbf01f44427d

  tensorboard:
    enabled: true

  types:
    master:
      count: 1
      resources:
        cpus: 1
        ramRatio: 2
        shmSizeGB: 0
    worker:
      count: 1
      resources:
        cpus: 1
        ramRatio: 2
        shmSizeGB: 0
